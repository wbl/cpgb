VPATH = ../../src/cpgb
IOPTS = -I../include
LOPTS = -L../lib -lnacl

%.o : %.c
	$(compiler) $(IOPTS) -c $? -o $@

% : %.o
	$(compiler) $(IOPTS) $(LOPTS) $? -o $@

all : keygen
keygen : keygen.o ../lib/randombytes.o
keygen.o : keygen.c

.PHONY clean:
	rm *